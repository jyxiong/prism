set(EXTERNAL_DIR "external")

if (NOT TARGET glfw)
    option(GLFW_BUILD_EXAMPLES "" OFF)
    option(GLFW_BUILD_TESTS "" OFF)
    option(GLFW_BUILD_DOCS "" OFF)
    option(GLFW_INSTALL "" OFF)
    add_subdirectory(glfw)
    set_target_properties(glfw PROPERTIES FOLDER ${EXTERNAL_DIR}/glfw)
    set_target_properties(update_mappings PROPERTIES FOLDER ${EXTERNAL_DIR}/glfw)
endif ()

if (NOT TARGET glm)
    option(BUILD_STATIC_LIBS "" ON)
    option(BUILD_TESTING "" OFF)
    option(GLM_TEST_ENABLE "" OFF)
    add_subdirectory(glm)
    # set_target_properties(glm_static PROPERTIES FOLDER ${EXTERNAL_DIR}/glm)
    # set_target_properties(uninstall PROPERTIES FOLDER ${EXTERNAL_DIR}/glm)
endif ()

if (NOT TARGET glslang)
    option(ENABLE_SPVREMAPPER OFF)
    option(ENABLE_HLSL OFF)
    option(ENABLE_OPT OFF)
    option(BUILD_TESTING OFF)
    option(BUILD_EXTERNAL OFF)
    add_subdirectory(glslang)
    set_target_properties(glslang PROPERTIES FOLDER ${EXTERNAL_DIR}/glslang)
endif ()

if (NOT TARGET imgui)
    include(imgui)
    set_target_properties(imgui PROPERTIES FOLDER ${EXTERNAL_DIR}/imgui)
endif ()

if (NOT TARGET spdlog)
    add_subdirectory(spdlog)
    set_target_properties(spdlog PROPERTIES FOLDER ${EXTERNAL_DIR}/spdlog)
endif ()

if (NOT TARGET stb)
    include(stb)
    set_target_properties(stb PROPERTIES FOLDER ${EXTERNAL_DIR}/stb)
endif ()

if (NOT TARGET volk)
    set(VULKAN_HEADERS_INSTALL_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Vulkan-Headers)
    add_subdirectory(volk)
    set_target_properties(volk PROPERTIES FOLDER ${EXTERNAL_DIR}/volk)
endif ()

if (NOT TARGET Vulkan-Headers)
    add_subdirectory(Vulkan-Headers)
    set_target_properties(Vulkan-Headers PROPERTIES FOLDER ${EXTERNAL_DIR}/Vulkan-Headers)
endif()

# if (NOT TARGET VulkanMemoryAllocator)
#     add_subdirectory(VulkanMemoryAllocator)
#     set_target_properties(VulkanMemoryAllocator PROPERTIES FOLDER ${EXTERNAL_DIR}/VulkanMemoryAllocator)
#     target_link_libraries(VulkanMemoryAllocator INTERFACE Vulkan-Headers)
# endif()